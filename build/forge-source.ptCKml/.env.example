# Copy only the names you need into your local launch environment. Never commit real keys.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-5.6-sol

# External web tools and preview updates intentionally have no environment bypass.
# Enable web research and select Stable/Preview explicitly in FORGE Settings.
