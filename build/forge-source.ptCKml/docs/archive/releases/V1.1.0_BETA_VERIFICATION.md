# 🗃️ FORGE 1.1.0-beta.1 Verification

> **Historical release record.** This former beta line was superseded and its release tag was removed. It remains audit evidence only; use the [current beta.2 verification](../../V2.1.0_BETA2_VERIFICATION.md) for supported release evidence.

This record distinguishes implemented behavior from release evidence. The `v1.1.0-beta.1` payload is verified at commit `8350aab8d498073b2335dfb8a1d7caa227865514` and was published as a GitHub Pre-release on August 7, 2026.

## Implemented source scope

- root-first workspace discovery and structured missing-path recovery;
- GPT-5.6 Responses API tool routing and provider-neutral direct-response support;
- workspace-owned PTY input, exit rejection, and writable restart;
- blank-file editor activation, save/open/undo/redo shortcuts, safe CLI environment, and keyless loopback model tools;
- schema-v4 persistent tasks, steps, dependencies, checkpoints, artifacts, approvals, external references, events, reconciliation, UI, and handoffs;
- Beta-channel discovery and legacy Preview preference migration;
- clean, manifest-driven ARM64/universal packaging, installation, and serial upload.

## Required provenance equality

The release evidence proves:

```text
v1.1.0-beta.1 tag target
= workflow head SHA
= packaged embedded commit
= installed embedded commit
```

That release payload commit is `8350aab8d498073b2335dfb8a1d7caa227865514`. After publication, `main` advanced to `4e8ee177e8840da3f1b7b0feb70e2b6a0019685e` only to merge the draft-release asset lookup repair from PR #15; the release tag was not moved or duplicated.

The universal DMG, ZIP, blockmaps, and `beta-mac.yml` must match the public asset sizes and SHA-256 values. The installed executable must originate from `/Applications/FORGE.app/Contents/MacOS/FORGE`, and no other installation or mounted FORGE DMG may remain.

## Acceptance gates

- dependency install, typecheck, lint, complete tests, production build;
- ARM64 and universal package creation from the intended commit;
- manifest, ZIP, DMG, updater metadata, architecture, signing, and provenance inspection;
- packaged `file://` app.asar renderer with no localhost dependency;
- manual terminal `pwd`, explicit output, exit rejection, and restart input;
- packaged `New file` activation/save plus Command-key save/open/undo/redo behavior;
- packaged `codex` resolution/launch after exact Gatekeeper trust and keyless Ollama model/file-tool routing;
- GPT-5.6 tool request through `/v1/responses` plus direct non-tool response;
- Tier 0 success, Tier 1 approval, Tier 2 approval/rejection, and linked audit records;
- root-first scan and recoverable `ENOENT` behavior;
- persistent task survival across renderer reload, application restart, conversation switch, and originating-conversation deletion;
- no repeated verified step and cross-workspace isolation;
- annotated tag, workflow, release, remote hashes, public download, public installation, diagnostics, and updater checks.

## Final observed evidence

- Clean dependency installation, typecheck, lint, 25 test files / 96 tests, production build, and `npm audit --omit=dev` with zero vulnerabilities passed.
- Exact release-commit ARM64 and universal packaging passed manifest verification, ZIP integrity, DMG verification, updater-metadata selection, and architecture inspection. The manifest recorded a clean source tree at `8350aab`.
- The installed packaged renderer reported `1.1.0-beta.1`, Beta, `file:// packaged app.asar`, and embedded commit `8350aab`.
- Packaged New File created `final-two.txt`, activated local Monaco, saved through Command+S, reopened a duplicate without raw `EEXIST`, and exercised single-dispatch undo/redo. Terminal Codex 0.147.0 and Ollama 0.32.1 executed with `TERM_PROGRAM=FORGE`; inactive terminal selection uses the repaired FORGE theme rather than opaque gray.
- Keyless `llama3.2:3b` requested `file.read` for `final-two.txt`. FORGE recorded exactly one automatic Tier 0 success in 3 ms and returned the observed `final-two-contentZ` value. Hosted-provider settings were restored afterward without clearing the configured key.
- The tag workflow passed checkout, dependency installation, the full source validation, draft creation, and unsigned universal packaging. Its first upload attempt failed because GitHub's tag API returned 404 for a draft release. PR #15 changed the lookup to draft-aware `gh release view`; the corrected serial uploader then uploaded the five manifest-selected assets, updater metadata last.
- Every downloaded public asset SHA-256 matched the local manifest and GitHub digest. The public universal ZIP was extracted and installed at exactly `/Applications/FORGE.app`; it contains x86_64 and arm64 and reports the tagged embedded commit. One physical FORGE application remains installed.
- The installed Beta-channel updater returned `not-available` at the same version and reported FORGE up to date; it did not offer an equal version, downgrade, alpha, or Stable-channel mismatch.

Signing and notarization are not configured. Gatekeeper reports no usable signature, so the beta requires a manual trust/install path and cannot support a trusted unattended replacement chain.
